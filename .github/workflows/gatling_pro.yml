name: Gatling Advanced Benchmark

on:
  workflow_dispatch:

jobs:
  gatling:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: ☕ Install Java + SBT
        uses: coursier/setup-action@v1
        with:
          jvm: temurin:11
          apps: sbt

      - name: 📦 Install system tools
        run: |
          sudo apt-get update
          sudo apt-get install -y sysstat gnuplot jq procps

      - name: 🧪 Start monitors
        run: |
          pidstat -durh 1 > pidstat.log &
          echo $! > pidstat_pid.txt

          (while true; do
             top -b -n1 | head -40 >> top.log
             echo "---" >> top.log
             sleep 3
           done) &
          echo $! > top_pid.txt

          (while true; do
             PID=$(pgrep -f "gatling" || true)
             if [ ! -z "$PID" ]; then
               ps -T -p "$PID" >> threads.log 2>/dev/null || true
             fi
             sleep 1
           done) &
          echo $! > threads_pid.txt

      - name: 🚀 Run Gatling
        run: |
          echo "Running Gatling..."
          cd gatling
          sbt "gatling:testOnly LoadTestSimulation" | tee ../gatling_stdout.txt

      - name: 🛑 Stop monitors
        if: always()
        run: |
          kill $(cat pidstat_pid.txt) || true
          kill $(cat top_pid.txt) || true
          kill $(cat threads_pid.txt) || true

      - name: 📊 Extract Gatling metrics
        run: |
          REPORT=$(find gatling/target/gatling -name simulation.log | head -1)
          if [ -z "$REPORT" ]; then
            echo "❌ No Gatling logs found"
            exit 1
          fi

          # p95 / p99 extract
          awk -F',' '/REQUEST/ {print $9}' "$REPORT" | sort -n | awk '
            NR==int(NR*0.95){print $1 > "p95.txt"}
            NR==int(NR*0.99){print $1 > "p99.txt"}
          '

          awk -F',' '/REQUEST/ {sum++} END{print sum/60 > "rps.txt"}' "$REPORT"

      - name: 📤 Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: gatling-results
          path: |
            gatling_stdout.txt
            pidstat.log
            top.log
            threads.log
            p95.txt
            p99.txt
            rps.txt
            gatling/target/gatling/**/*
